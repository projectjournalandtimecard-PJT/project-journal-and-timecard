{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core capabilities for the main window, the focus-timer popout and the guide window.",
  "windows": ["main", "tinkers-bell", "pjt-guide"],
  "permissions": [
    "core:default",
    "core:webview:allow-create-webview-window",
    "core:window:allow-set-size",
    "core:window:allow-center"
  ]
}
