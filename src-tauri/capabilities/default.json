{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core capabilities for the main window and the focus-timer popout.",
  "windows": ["main", "tinkers-bell", "pjt-guide"],
  "permissions": [
    "core:default",
    "core:webview:allow-create-webview-window",
    "core:window:allow-set-size",
    "core:window:allow-center"
  ]
}
