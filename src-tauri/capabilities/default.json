{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core capabilities for the main window and the tinker's bell popout.",
  "windows": ["main", "tinkers-bell"],
  "permissions": [
    "core:default",
    "core:webview:allow-create-webview-window"
  ]
}
